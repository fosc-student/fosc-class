#include <remembering the 11th.pdf>
//for the feels

#include "*-14"
//for the knowledges

#include <transparency>
//for the thesis statement

#include <professor>
//for the lulz

int main(int argc int[] args){
    professor Amit = new professor(Triskadekaphobia);
    transparency Nando = new transparency(thoughts);
    file[] notes("*-14");
    Amit.keep_in_mind("remembering the 11th");
    while(notes[i].read(false)):  //loop through all the notes files
        Amit.reflect_upon(notes[i]);
        Nando.add(notes[i]);
        Nando.compare(notes[i], Nando.feelings, "remembering the 11th")
        notes[i].read = true;
        i = i + 1;              // increment to the next note file
    }
    Amit.project_submissions.add(Nando.submit(comparison));
    return Amit.transparency(thoughts);
